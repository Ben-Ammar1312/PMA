test:
  stage: test
  image: maven:3.9-amazoncorretto-21
  variables:
    SPRING_PROFILES_ACTIVE: cicd
    FILE_UPLOAD_DIR: uploads
  script:
    - mvn clean test -Dspring.profiles.active=cicd
  tags:
    - docker
  only:
    - dev
    - merge_requests
