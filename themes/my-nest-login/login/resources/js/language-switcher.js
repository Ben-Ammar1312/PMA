// language-switcher.js
document.getElementById('lang-toggle')?.addEventListener('click', () => {
  // implement your locale switch (e.g. call Keycloak’s setLocale)
  alert('Language toggle clicked');
});